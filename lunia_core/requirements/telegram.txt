# Optional Telegram stack
#
# Upgrade aiogram to the latest stable 3.x release and align aiohttp accordingly.
# These packages are optional and only needed when enabling Telegram support.
aiogram==3.4.1
aiohttp>=3.9
